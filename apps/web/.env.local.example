# Development only, never committed with a real value.
# Generate with: pnpm --filter @tma/api tg:sign-initdata -- --user-id <your telegram id>
# The API validates the signature of this string, so it is a fixture, not a bypass.
NEXT_PUBLIC_TG_MOCK_INIT_DATA=
