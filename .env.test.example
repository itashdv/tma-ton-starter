# Copy to .env.test. Integration tests migrate and TRUNCATE this database on every run,
# so it must never point at a database with real data.
TEST_DATABASE_URL=postgres://tma:tma@127.0.0.1:5432/tma_test
